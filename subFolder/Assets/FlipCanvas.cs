using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FlipCanvas : MonoBehaviour {

	// Use this for initialization
	void Start () {
        this.transform.Rotate(0, 0, 180.0f);
    }
	
	// Update is called once per frame
	void Update () {
     
	}
}
